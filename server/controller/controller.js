const User = require('../model/user');


const postUserData = (req, res)=>{
    try {
        
    } catch (error) {
        
    }
}

const getUserData=(req, res)=>{
    try {
        
    } catch (error) {
        
    }
}

module.exports={
    postUserData,
    getUserData,
}